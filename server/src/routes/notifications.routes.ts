import { Router } from 'express';

const router = Router();

// Placeholder for notifications routes
router.get('/', (req, res) => {
  res.json({ success: true, message: 'Notifications API placeholder' });
});

export default router;
