import { Router } from 'express';

const router = Router();

// Placeholder for social routes
router.get('/', (req, res) => {
  res.json({ success: true, message: 'Social API placeholder' });
});

export default router;
